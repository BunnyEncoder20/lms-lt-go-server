info:
  name: get courses stats
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/admin/courses/stats"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
