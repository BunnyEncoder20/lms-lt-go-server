info:
  name: Manager Login
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/login"
  body:
    type: json
    data: |-
      {
          "Email": "manager@skillsync.com",
          "Password": "Manager123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
