info:
  name: get hr feed
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/notifications/feed?limit=30"
  params:
    - name: limit
      value: "30"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
