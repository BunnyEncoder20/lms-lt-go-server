info:
  name: get upcoming trainings
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/trainings/upcoming"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
