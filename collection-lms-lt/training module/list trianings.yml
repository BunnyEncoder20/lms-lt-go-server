info:
  name: list trianings
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/trainings"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
