info:
  name: get admin kpis
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/admin/kpis"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
