info:
  name: get all users
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/admin/users"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
