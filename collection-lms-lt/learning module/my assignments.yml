info:
  name: my assignments
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/learning/my"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
