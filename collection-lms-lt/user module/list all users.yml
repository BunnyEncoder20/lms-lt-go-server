info:
  name: list all users
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/admin/users"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
